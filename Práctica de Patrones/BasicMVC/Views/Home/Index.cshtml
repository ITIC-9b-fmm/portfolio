@model IEnumerable<BasicMVC.Models.Coche>

<h2>Listado de Coches</h2>
@*Esta es la parte de la Vista, donde se mostrarán los coches
    instanciados en el Controlador*@
<div>
    @{ 
        var grid = new WebGrid(Model, defaultSort: "Marca");
    }

    @grid.GetHtml(columns: grid.Columns(
    grid.Column("Marca", header:"Marca"),
    grid.Column("Modelo", header:"Modelo"),
    grid.Column("Color", header:"Color")
    ))
</div>